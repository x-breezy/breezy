services:
  gateway:
    build:
      context: .
      dockerfile: infrastructure/gateway/Dockerfile
    container_name: breezy-gateway
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      AUTH_UPSTREAM: host.docker.internal:4020
      PROFILE_UPSTREAM: host.docker.internal:4010
      POST_UPSTREAM: host.docker.internal:4040
      MEDIA_UPSTREAM: host.docker.internal:4050
      NOTIFICATIONS_UPSTREAM: host.docker.internal:4060
      MESSAGE_UPSTREAM: host.docker.internal:4030
      WEB_UPSTREAM: host.docker.internal:3000
      SWAGGER_UPSTREAM: swagger-ui:8080
      KIBANA_UPSTREAM: 127.0.0.1:5601
      RABBITMQ_UPSTREAM: rabbitmq:15672
      CORS_ORIGIN_1: http://localhost:3000
      CORS_ORIGIN_2: http://localhost:3001
      LOGS_DOMAIN: logs.localhost
      MQ_DOMAIN: mq.localhost
    networks:
      - breezy

  mongo-post-service:
    image: mongo:8
    container_name: breezy-mongo-post-service
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: post_service
    volumes:
      - mongo-post-service-data:/data/db
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-media-service:
    image: mongo:8
    container_name: breezy-mongo-media-service
    restart: unless-stopped
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_DATABASE: media_service
    volumes:
      - mongo-media-service-data:/data/db
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-user-service:
    image: postgres:16
    container_name: breezy-postgres-user-service
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: user_service
      POSTGRES_HOST_AUTH_METHOD: md5
      POSTGRES_INITDB_ARGS: "--auth-host=md5 --auth-local=md5"
    volumes:
      - postgres-user-service-data:/var/lib/postgresql/data
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres-profile-service:
    image: postgres:16
    container_name: breezy-postgres-profile-service
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: profile_service
      POSTGRES_HOST_AUTH_METHOD: md5
      POSTGRES_INITDB_ARGS: "--auth-host=md5 --auth-local=md5"
    volumes:
      - postgres-profile-service-data:/var/lib/postgresql/data
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: breezy-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management
    container_name: breezy-rabbitmq
    restart: unless-stopped
    ports:
      - "127.0.0.1:5672:5672"
      - "127.0.0.1:15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: breezy
      RABBITMQ_DEFAULT_PASS: breezy
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-notifications-service:
    image: mongo:8
    container_name: breezy-mongo-notifications-service
    restart: unless-stopped
    ports:
      - "27019:27017"
    environment:
      MONGO_INITDB_DATABASE: notifications_service
    volumes:
      - mongo-notifications-service-data:/data/db
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-message-service:
    image: mongo:8
    container_name: breezy-mongo-message-service
    restart: unless-stopped
    ports:
      - "27020:27017"
    environment:
      MONGO_INITDB_DATABASE: message_service
    volumes:
      - mongo-message-service-data:/data/db
    networks:
      - breezy
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
  swagger-ui:
    build:
      context: .
      dockerfile: infrastructure/docs/Dockerfile
    container_name: breezy-swagger-ui
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - breezy

  # sonarqube:
  #   image: sonarqube:10.6-community
  #   container_name: breezy-sonarqube
  #   restart: unless-stopped
  #   environment:
  #     - SONAR_ES_BOOTSTRAP_CHECKS_DISABLE=true
  #     - SONAR_SEARCH_JAVAOPTS=-Xms512m -Xmx512m
  #     - SONAR_WEB_JAVAOPTS=-Xmx512m
  #     - SONAR_JDBC_URL=jdbc:postgresql://postgres-sonar:5432/sonar
  #     - SONAR_JDBC_USERNAME=sonar
  #     - SONAR_JDBC_PASSWORD=sonar
  #   ports:
  #     - "9000:9000"
  #   depends_on:
  #     postgres-sonar:
  #       condition: service_healthy
  #   networks:
  #     - breezy
  #   volumes:
  #     - sonarqube-data:/opt/sonarqube/data
  #     - sonarqube-logs:/opt/sonarqube/logs
  #   mem_limit: 2g

  # postgres-sonar:
  #   image: postgres:16-alpine
  #   container_name: breezy-postgres-sonar
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: sonar
  #     POSTGRES_PASSWORD: sonar
  #     POSTGRES_DB: sonar
  #   volumes:
  #     - postgres-sonar-data:/var/lib/postgresql/data
  #   networks:
  #     - breezy
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U sonar"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

networks:
  breezy:
    driver: bridge

volumes:
  mongo-post-service-data:
  mongo-media-service-data:
  mongo-notifications-service-data:
  mongo-message-service-data:
  swagger-ui:
  postgres-user-service-data:
  postgres-profile-service-data:
  postgres-message-service-data:
  # sonarqube-data:
  # sonarqube-logs:
  # postgres-sonar-data:
